version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install --no-cache-dir -r requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  env:
    - name: ALGORITHM
      value: "HS256"
    - name: EMAIL_PORT
      value: "587"
