# ============================================================
# Landa Beauty Supply API - Environment Variables
# ============================================================
# Copy this file to .env.dev (development) or .env.prod (production)
# and fill in your values.
#
# Usage:
#   Development: set ENV_FILE=.env.dev (default)
#   Production:  set ENV_FILE=.env.prod
# ============================================================

# ------------------------------------------------------------
# JWT Authentication
# ------------------------------------------------------------
# Secret key for signing JWT tokens (use a long random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-here-change-this

# JWT algorithm (HS256 recommended)
ALGORITHM=HS256

# ------------------------------------------------------------
# Email Configuration (for password reset)
# ------------------------------------------------------------
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# Frontend URL for password reset links
FRONTEND_RESET_URL=http://localhost:3000/reset-password?token=

# Max password reset requests per hour (rate limiting)
PASSWORD_RESET_MAX_REQUESTS_PER_HOUR=3

# ------------------------------------------------------------
# Stripe Configuration (Payments)
# ------------------------------------------------------------
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ------------------------------------------------------------
# Admin OAuth2 App (Dashboard Access)
# ------------------------------------------------------------
# If set, a super admin app will be auto-created on startup
# with these credentials. You can then use them to access
# /admin/* endpoints.
#
# Generate secure values:
#   client_id:     app_<random> (e.g., app_production_admin)
#   client_secret: sk_live_<64 random chars>
#                  python -c "import secrets; print('sk_live_' + secrets.token_hex(32))"
#
# Leave empty to skip auto-creation (use create_admin_app.py instead)
ADMIN_CLIENT_ID=app_landa_admin
ADMIN_CLIENT_SECRET=sk_live_replace_with_64_char_random_string_here

# ------------------------------------------------------------
# Single Access Token Configuration (User Magic Links)
# ------------------------------------------------------------
# Base URL for the wholesale frontend (used in access links)
WHOLESALE_FRONTEND_URL=https://wholesale.landabeautysupply.com

# Token expiration time in hours
SINGLE_ACCESS_TOKEN_EXPIRE_HOURS=24
